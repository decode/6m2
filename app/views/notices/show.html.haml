.well
  %p
    %b= t 'global.title'
    = @notice.title if @notice.title
  %p
    %b= t 'global.content'
    = @notice.content if @notice.content
%p
  - if admin?
    = link_to t('global.edit'), edit_notice_path(@notice)
    |
  = link_to t('global.back'), notices_path
