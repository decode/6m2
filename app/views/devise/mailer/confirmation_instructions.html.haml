%h1= t 'devise.no_reply'

%p= t 'site.welcome', :name => "#{@resource.email}!"
%p= t 'devise.confirm_desc'
%p= link_to t('devise.confirm'), confirmation_url(@resource, :confirmation_token => @resource.confirmation_token)
%p= t 'devise.not_link'
%p= confirmation_url(@resource, :confirmation_token => @resource.confirmation_token)
