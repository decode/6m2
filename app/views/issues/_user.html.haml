.bordershape
  = t 'issue.issue_person'
  - if (admin? or manager?) and !@user.nil?
    = @user.username
    = t 'account.account_money'
    = @user.account_money
    = t 'account.point'
    = @user.account_credit
    = t 'trade.payment_amount'
    = @user.payment_money
    %hr
  
  - unless @task.nil?
    = t('formtastic.labels.participant.part_id') + ": "
    = @task.worker_part_name if @task.worker_part_name
    =# @task.participant.part_id if @task.participant
