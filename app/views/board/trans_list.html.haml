.alert.alert-info
  = t('transaction.length') + @trans.length.to_s
  = t('simple_form.labels.transaction.amount') + @trans.sum('amount').to_s
  = t('simple_form.labels.transaction.point') + @trans.sum('point').to_s

#wide
  = render 'transactions/trans'

